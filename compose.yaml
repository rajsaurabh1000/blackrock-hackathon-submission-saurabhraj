# Docker Compose (file naming per challenge: compose.yaml)
# Usage: docker compose up -d
# Application runs on port 5477; no external services required.
services:
  app:
    build: .
    image: blackrock-challenge
    ports:
      - "5477:5477"
    environment:
      - PORT=5477
